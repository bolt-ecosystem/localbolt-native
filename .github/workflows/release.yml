name: Release (native path pending)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired-tauri-release:
    name: Retired Tauri release guard
    runs-on: ubuntu-latest
    steps:
      - name: Block retired release path
        run: |
          echo "::error::The old Tauri release workflow is retired."
          echo "localbolt-app now ships native shells over shared Rust core."
          echo "Use native/macos/build-app.sh for macOS artifacts until native release automation is codified."
          exit 1
